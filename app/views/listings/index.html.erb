<p id="notice"><%= notice %></p>

<div class="center">
  <div class="row">
    <% @listings.shuffle.each do |listing| %>
      <div class="col-md-3">
        <div class="thumbnail">
          <%#= image_tag listing.image.url %>
          <%= link_to listing_path(listing) do %>
              <%= image_tag listing.image.url%>
          <% end %>
          <!-- <div class="caption"> -->
            <!-- <h3><%#= listing.name_english %>&nbsp&nbsp<%#= number_to_currency((listing.price_euro / 100.0), unit: "€" ) %></h3> -->
            <!-- <h3><%#= listing.name_spanish %></h3> -->
            <!-- <p>Product code: <%#= listing.id %></p> -->

            <%#= link_to 'Show', listing, class: "btn btn-link" %>
          <!-- </div> -->
        </div>
      </div>
    <% end %>
  </div>
</div>

<br>


<% if !current_user.nil? && current_user.has_manager %>
  <%= link_to 'New Product', new_listing_path, class: "btn btn-link" %>
<% end %>


